describe('position', () => {
  it('get position of content box by x axis with CSS "content-box"', () => {
    // expect().toEqual()
  })

  it('get position of content box by y axis with CSS "content-box"', () => {
    // expect().toEqual()
  })

  it('get position of content box by x axis with CSS "border-box"', () => {
    // expect().toEqual()
  })

  it('get position of content box by y axis with CSS "border-box"', () => {
    // expect().toEqual()
  })
})

describe('size', () => {
  it('get width of content box with CSS "content-box"', () => {
    // expect().toEqual()
  })

  it('get height of content box with CSS "content-box"', () => {
    // expect().toEqual()
  })

  it('get width of content box with CSS "border-box"', () => {
    // expect().toEqual()
  })

  it('get height of content box with CSS "border-box"', () => {
    // expect().toEqual()
  })
})
