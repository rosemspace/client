import App from '@rosem/vue-app'
// import config from './app.config'
// import { createProvider } from './vue-apollo'

// let arr = [1,2,3,4];
// arr.length = 3;
// console.log(arr);
// console.log(arr.map((num, index) => {console.log(num, arr[index + 1]); return num}))
App.boot();

